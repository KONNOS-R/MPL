[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"


[project]
name = "mpl"
version = "0.1.0"
description = "Music Playlist (MPL) format handler"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]